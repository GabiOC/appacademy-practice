/* PROBLEM
Write a method that takes in a string. Your method should return the most common letter in the array,
and a count of how many times it appears.

PSEUDO CODE
problem summary: return the most common letter in string, and how many times it appears
example: most_common_letter("abca") == ["a", 2]
 */

function mostCommonLetter(arr) {
//     WIP below

// 	   var mostCommon = [];
//     var max = 0;
//     var count = 0;

//     arr.split("").forEach(function(i){
//         if(mostCommon[i]){
//         	mostCommon[i]++; // if it already exists in hash, count for the key is incremented 
//         }
//         else{
//         	mostCommon[i] = 1;
//         } 
//         if(count < mostCommon[i]){ // checks to see if the existing highest mode (var count) is greater than the element just processed
//             max = i;
//             count = mostCommon[i];
//         }
//     });
    
//     return max + "," + count;
// }

console.log(mostCommonLetter("abca"));